import { objectReviewActions } from '../constants';

const setCurrentDeal = deal => dispatch => {
  // dispatch({
  //     type: dealActions.setCurrentDeal,
  //     payload: deal,
  // });
};

export default {
  setCurrentDeal,
};
